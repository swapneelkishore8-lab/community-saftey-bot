{
  "buildCommand": "uv pip install -r requirements.txt --system",
  "outputDirectory": "public",
  "framework": "flask",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index.py"
    }
  ]
}
